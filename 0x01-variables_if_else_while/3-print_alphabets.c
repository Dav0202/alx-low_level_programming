#include <stdio.h>
/**
 * main - alphabet in lowercase and upper, followed by a new line.
 * Return: Always(0)
 */
int main(void)
{
	char ch1 = 'a';

	char ch2 = 'A';

	for (ch1 = 'a'; ch1 <= 'z'; ch1++)
	{
		putchar(ch);
	}
	for (ch2 = 'A'; ch2 <= 'Z'; ch2++)
	{
		putchar(ch2);
	}
	putchar('\n');
	return (0);
}
